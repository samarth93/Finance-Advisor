spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: expense_tracker_test
  
logging:
  level:
    com.expensetracker: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.boot.test: DEBUG

# Test-specific configurations
test:
  user:
    sample-user-id: testuser123
    sample-email: test@example.com
    sample-name: Test User
  category:
    sample-category: Food
  expense:
    sample-amount: 100.50
